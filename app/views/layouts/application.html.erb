<!DOCTYPE html>
<html>
    <head>
        <title>Critique</title>
        <%=favicon_link_tag%>
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>
        <%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>
        <%= stylesheet_link_tag "//cdn.datatables.net/v/bs/jq-2.2.3/dt-1.10.12/datatables.min.css" %>
        <%= javascript_include_tag "//cdn.datatables.net/v/bs/jq-2.2.3/dt-1.10.12/datatables.min.js" %>
    </head>
    <body>
        <div id="wrapper">
            <%= render 'shared/navbar' %>
            <div class="padding"></div>
            <div class="maincontent">
                <div id="flashMsg">
                    <% flash.each do |type, message| %>
                        <div class="flash alert <%= bootstrap_class_for(type) %> fade in">
                            <button class="close" data-dismiss="alert">×</button>
                            <%= message %>
                        </div>
                    <% end %>
                </div>
                <%= yield %>
            </div>
            <div class="padding"></div>
            <%= render 'shared/footer' %>
        </div>
    </body>
</html>
